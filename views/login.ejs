<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login Page</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="../css/style.css">
</head>
<body class="bg-white bg-light d-flex align-items-center justify-content-center min-vh-100 mx-5">
  <div class="shadow-lg border-0 w-100" style="max-width: 720px;">
    <div class="row g-0">
      <div class="col-lg-6 d-none d-lg-block login-image rounded-start"></div>
      <div class="col-lg-6 p-4">
        <h1 class="text-center fs-3 fw-semibold mb-2">BlogSphere</h1>
        <p class="text-center text-muted fs-5 mb-4">Welcome back!</p>
        <form method="POST" action="/api/auth/login">
          <div class="mb-3">
            <label class="form-label fw-semibold">Email Address</label>
            <input type="email" name="email" required class="form-control bg-light border-secondary-subtle" />
          </div>
          <div class="mb-4">
            <label class="form-label fw-semibold">Password</label>
            <input type="password" name="password" required class="form-control bg-light border-secondary-subtle" />
          </div>
          <% if (error) { %>
            <p class="alert alert-danger text-center py-2"><%= error %></p>
          <% } %>
          <div class="d-grid mb-3">
            <button type="submit" class="btn btn-dark fw-bold">Login</button>
          </div>
        </form>
        <div class="d-flex align-items-center justify-content-between mt-3">
          <hr class="w-25">
          <a href="/" class="text-uppercase text-muted small text-center text-decoration-none">or Enter as Guest</a>
          <hr class="w-25">
        </div>
      </div>
    </div>
  </div>
</body>
</html>
